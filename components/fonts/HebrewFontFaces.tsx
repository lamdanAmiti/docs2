/**
 * Hebrew fonts are wired in via app/hebrew-fonts.css (generated from
 * lib/fonts/hebrew-fonts.ts). This module is intentionally a no-op
 * placeholder so any future runtime font injection has a home.
 */
export function HebrewFontFaces() {
  return null;
}
